{
  // Prefer the system Python on PATH so this repo can be moved without depending on a workspace venv.
  // If you want a project venv later, create .venv/ and select it with the interpreter selector.
  "python.defaultInterpreterPath": "python"
}
